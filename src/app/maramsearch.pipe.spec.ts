import { MaramsearchPipe } from './maramsearch.pipe';

describe('MaramsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MaramsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
