import { MaramcolorDirective } from './maramcolor.directive';

describe('MaramcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaramcolorDirective();
    expect(directive).toBeTruthy();
  });
});
